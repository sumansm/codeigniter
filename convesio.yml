import:
  - convesio/codeigniter
  - source: convesio/mysql
     delegates:
       phpmyadmin: null

ui:
  inputs:
    - name: root_password
      label: MYSQL ROOT USER
      type: text
      required: true
      default: SUMAN
      description: PASSWORD FOR MYSQL ROOT USER
      size: 1x

